{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h1>Produits</h1>

    <table class="table table-striped table-hover" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entity.prix }}</td>
            </tr>
            <tr>
                <th>Qantite</th>
                <td>{{ entity.quantite }}</td>
            </tr>
            <tr>
                <th>Promotion</th>
                <td>{{ entity.promotion }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ entity.marque }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ entity.disponible }}</td>
            </tr>
        </tbody>
    </table>

    <dl class="record_actions">
        <dt>
            <a href="{{ path('adminProduits') }}" class="btn">
                Retour à la liste
            </a>
        </dt></br>
        <dt>
            <a href="{{ path('adminProduits_edit', { 'id': entity.id }) }}" class="btn">
                Modifier
            </a>
        </dt></br>
        <dt>{{ form(delete_form) }}</dt>
    </dl>
{% endblock %}
