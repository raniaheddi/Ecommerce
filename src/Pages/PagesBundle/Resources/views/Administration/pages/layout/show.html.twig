{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Pages</h1>

    <table class="table table-striped table-hover" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ entity.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ entity.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <dl class="record_actions">
        <dt>
            <a href="{{ path('adminPages') }}" class="btn">
                Retour à la liste
            </a>
        </dt></br>
        <dt>
            <a href="{{ path('adminPages_edit', { 'id': entity.id }) }}" class="btn">
                Modifier
            </a>
        </dt></br>
        <dt>{{ form(delete_form) }}</dt>
    </ul>
{% endblock %}
